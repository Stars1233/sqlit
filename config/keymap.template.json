{
  "_note": "Copy to ~/.sqlit/keymaps/<name>.json and set 'custom_keymap' to '<name>' in settings.json.",
  "_help": "Only list the bindings you want to change. Each entry maps action → key (or [primary, alias...]) within its state. See docs/custom-keymaps.md for the action/state catalog.",
  "keymap": {
    "action_keys": {
      "query_normal": {
        "execute_query": "ctrl+enter"
      },
      "autocomplete": {
        "autocomplete_next": "ctrl+n",
        "autocomplete_prev": "ctrl+p"
      },
      "tree": {
        "tree_cursor_down": ["j", "down"],
        "tree_cursor_up": ["k", "up"]
      }
    },
    "leader_commands": {
      "leader": {
        "show_help": "question_mark"
      }
    }
  }
}
